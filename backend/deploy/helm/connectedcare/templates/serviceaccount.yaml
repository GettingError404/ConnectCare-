{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "connectedcare.serviceAccountName" . }}
  labels:
    {{- include "connectedcare.labels" . | nindent 4 }}
{{- end -}}
